<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.Grammar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <style name="Theme.Grammar" parent="Base.Theme.Grammar" />

    <style name="Widget.Grammar.Button.BoldBorder" parent="Widget.Material3.Button.TonalButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">60dp</item>

        <item name="strokeColor">#BDBDBD</item>
        <item name="strokeWidth">1dp</item>
        <item name="cornerRadius">6dp</item>

        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
<!--        <item name="textFontWeight" tools:targetApi="p">700</item>-->
        <item name="android:textAllCaps">false</item>

        <item name="backgroundTint">?attr/colorPrimary</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="iconTint">@android:color/white</item>
    </style>

    <style name="RectButtonStyle" parent="Widget.Grammar.Button.BoldBorder">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginBottom">12dp</item>
    </style>

    <style name="Theme.Grammar.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>